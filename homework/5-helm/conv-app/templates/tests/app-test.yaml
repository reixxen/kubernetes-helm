apiVersion: v1
kind: Pod
metadata:
  name: {{ .Release.Name }}-app-test
  labels:
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ["wget"]
      args: ["{{ .Values.app.name }}-service:{{ .Values.app.service.ports.http }}"]
  restartPolicy: Never
